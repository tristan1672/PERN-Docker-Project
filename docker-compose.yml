version: '3.8'
services:
  database:
    image: postgres:15
    container_name: ED-database
    restart: always
    #env_file:
    #  - .env
    environment:
      POSTGRES_DB: csv_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - app-network

  backend:
    build: ./backend
    container_name: ED-backend
    restart: always
    depends_on:
      - database
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/backend
    command: npm run dev
    environment:
      - DATABASE_URL=postgres://user:password@database:5432/csv_db
      - PORT=5000
      - HOST=0.0.0.0
    networks:
      - app-network

volumes:
  node_modules:
  pgdata:

networks:
  app-network:
    driver: bridge